{
    "name": "linnet-lambda-upsert",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "clean": "rimraf ./lib ./.nyc_output ./coverage",
        "build": "yarn clean && tsc -p tsconfig.json"
    },
    "dependencies": {
        "aws-xray-sdk-core": "^1.2.0"
    }
}
